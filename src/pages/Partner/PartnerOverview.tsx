import { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const PartnerOverview = () => {
    const navigate = useNavigate();
    return (
        <div>PartnerOverview</div>
    )
}

export default PartnerOverview;